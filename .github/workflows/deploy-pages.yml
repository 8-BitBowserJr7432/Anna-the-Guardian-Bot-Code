# ─────────────────────────────────────────────────────────────
# 🛡️ Anna — The Guardian Bot
# GitHub Pages Deployment Workflow
#
# “I stand watch. I build. I deploy.” — Anna
#
# This workflow automatically deploys the web demo
# (HTML / CSS / JS) to GitHub Pages on every push to main.
# ─────────────────────────────────────────────────────────────

name: 🛡️ Deploy Anna Guardian Web

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    name: 🌐 Guarded Deployment
    runs-on: ubuntu-latest

    steps:
      # ─────────────────────────────────────────
      # 🧾 Step 1 — Fetch repository
      # ─────────────────────────────────────────
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # ─────────────────────────────────────────
      # 🛡️ Step 2 — Validate site files
      # ─────────────────────────────────────────
      - name: 🔍 Anna scans for index.html
        run: |
          if [ ! -f index.html ]; then
            echo "❌ Anna Alert: index.html not found!"
            exit 1
          fi
          echo "✅ Anna Approved: index.html detected."

      # ─────────────────────────────────────────
      # 📦 Step 3 — Upload site artifact
      # ─────────────────────────────────────────
      - name: 📦 Package Guardian Web
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      # ─────────────────────────────────────────
      # 🚀 Step 4 — Deploy to GitHub Pages
      # ─────────────────────────────────────────
      - name: 🚀 Deploy with Anna
        id: deployment
        uses: actions/deploy-pages@v4

# ─────────────────────────────────────────────────────────────
# Deployment complete.
# Anna remains vigilant.
# ─────────────────────────────────────────────────────────────
